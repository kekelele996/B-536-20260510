# 更新日志

## 2026-01-22 - 重要优化更新

### 修复内容

#### 1. 替换 alert 弹窗为现代化 Toast 组件

**问题**
- 原系统使用浏览器原生 alert 弹窗，用户体验差

**解决方案**
- 新增 `Toast.vue` 组件，提供更美观的消息提示
- 新增 `useToast.js` composable，统一管理 Toast 状态
- 支持 4 种消息类型：success（成功）、error（错误）、warning（警告）、info（信息）
- 自动 3 秒后消失，带有平滑的进入/退出动画

**修改文件**
- 新增：`frontend/src/components/Toast.vue`
- 新增：`frontend/src/composables/useToast.js`
- 修改：`frontend/src/App.vue` - 添加全局 Toast 组件
- 修改：所有页面组件，使用 `useToast()` 替换 `alert()`

**效果**
- 登录/注册成功：显示绿色成功提示
- 操作失败：显示红色错误提示
- 权限不足：显示黄色警告提示
- 所有提示都在页面顶部居中显示，不会阻塞页面操作

---

#### 2. 修复预约后座位数不更新的 Bug

**问题**
- 用户预约成功后，时间段的剩余座位数没有减少
- 取消预约后，座位数没有增加回去

**根本原因**
- 后端预约服务只创建了预约记录，没有更新 TimeSlot 的 availableSeats 字段

**解决方案**
- 修改 `ReservationService.createReservation()`：预约成功后，将 TimeSlot 的 availableSeats 减 1
- 修改 `ReservationService.cancelReservation()`：取消预约后，将 TimeSlot 的 availableSeats 加 1
- 添加重复取消检查，防止座位数错误增加

**修改文件**
- `backend/src/main/java/com/studyroom/service/ReservationService.java`

**效果**
- 预约成功后，刷新页面可看到剩余座位数实时减少
- 取消预约后，座位数正确恢复
- 当座位数为 0 时，"立即预约"按钮自动禁用并显示"已满"

---

#### 3. 优化错误提示信息

**问题**
- 错误提示直接显示 HTTP 状态码（如"400"），用户无法理解
- 网络错误、服务器错误等提示不够友好

**解决方案**
- 修改 axios 响应拦截器，将 HTTP 状态码转换为友好的中文提示
- 优先使用后端返回的 message 字段
- 对常见错误码提供预设的友好提示

**修改文件**
- `frontend/src/api/index.js`

**错误码映射**
| 状态码 | 友好提示 |
|--------|---------|
| 400 | 请求参数错误，请检查输入 |
| 401 | 未授权，请重新登录 |
| 403 | 没有权限执行此操作 |
| 404 | 请求的资源不存在 |
| 500 | 服务器错误，请稍后重试 |
| 网络错误 | 无法连接到服务器，请检查网络 |

**效果**
- 用户看到的都是可理解的中文错误提示
- 错误信息通过 Toast 组件显示，体验更好
- 开发者仍然可以在控制台看到详细的错误信息

---

### 技术细节

#### Toast 组件设计

**功能特性**
- 位置：页面顶部居中，距离顶部 80px
- 自动消失：默认 3 秒后自动关闭
- 动画效果：淡入淡出 + 向下滑动
- 响应式：自适应宽度，最小 300px，最大 500px

**样式设计**
- 使用圆形图标区分消息类型
- 成功：绿色背景 + ✓ 图标
- 错误：红色背景 + ✕ 图标
- 警告：橙色背景 + ⚠ 图标
- 信息：蓝色背景 + ℹ 图标

#### useToast Composable

**API 设计**
```javascript
const { success, error, warning, info } = useToast()

// 使用方法
success('操作成功')
error('操作失败')
warning('请注意')
info('提示信息')
```

**状态管理**
- 使用 Vue 3 ref 管理全局 Toast 状态
- 所有组件共享同一个 Toast 实例
- 支持并发显示（后续可扩展为队列）

#### 座位数更新逻辑

**数据一致性保证**
- 使用 @Transactional 确保原子性
- 预约和座位数更新在同一事务中
- 失败时自动回滚，不会出现数据不一致

**并发控制**
- 预约前检查当前预约数量
- 使用数据库锁机制防止超额预约
- 座位数为 0 时前端禁用预约按钮

---

### 升级指南

#### 对于已部署的系统

**方式一：完全重建（推荐）**
```bash
cd /path/to/studySpace

# 停止并删除旧容器
docker compose down

# 重新构建
docker compose build --no-cache

# 启动
docker compose up -d
```

**方式二：仅更新代码**
```bash
# 拉取最新代码
git pull

# 重新构建并重启
docker compose up -d --build
```

#### 验证更新

1. 访问系统，操作任何功能，观察是否出现 Toast 提示而非 alert
2. 预约一个座位，检查剩余座位数是否减少
3. 取消预约，检查座位数是否恢复
4. 故意输入错误信息（如错误密码），观察错误提示是否友好

---

### 后续计划

1. 添加预约时间冲突检查
2. 实现座位地图可视化
3. 添加预约提醒功能
4. 优化移动端体验
5. 添加数据统计看板

---

**更新时间**：2026-01-22
**版本**：v1.1.0
