# 修复重复预约问题

## 问题描述

用户可以对同一自习室的同一时间段进行多次预约，这导致：
- 一个用户占用多个座位
- 座位资源浪费
- 数据不合理

## 解决方案

### 后端修复

#### 1. 添加查询方法

在 `ReservationRepository.java` 中新增查询方法：

```java
@Query("SELECT COUNT(r) FROM Reservation r WHERE r.userId = ?1 AND r.timeSlotId = ?2 AND r.status = 'ACTIVE'")
long countActiveReservationsByUserAndTimeSlot(Long userId, Long timeSlotId);
```

#### 2. 添加重复预约检查

在 `ReservationService.createReservation()` 中添加检查逻辑：

```java
// 检查用户是否已经预约了这个时间段
long userReservationCount = reservationRepository.countActiveReservationsByUserAndTimeSlot(
        request.getUserId(), request.getTimeSlotId());
if (userReservationCount > 0) {
    throw new BusinessException("您已经预约了该时间段，请勿重复预约");
}
```

**检查顺序：**
1. 先检查用户是否已预约（避免无意义的座位检查）
2. 再检查座位是否已满

---

### 前端优化

#### 1. 显示已预约状态

在时间段列表页面：
- 加载用户的所有活跃预约
- 检查每个时间段是否已被当前用户预约
- 已预约的时间段按钮显示"已预约"并禁用

#### 2. 按钮状态优化

| 状态 | 按钮文字 | 按钮颜色 | 是否禁用 |
|------|---------|---------|---------|
| 可预约 | 立即预约 | 绿色 | 否 |
| 座位已满 | 已满 | 红色 | 是 |
| 已预约 | 已预约 | 红色 | 是 |

#### 3. 实时状态更新

- 预约成功后，立即刷新时间段列表和预约记录
- 按钮状态实时更新为"已预约"
- 用户无需刷新页面就能看到最新状态

---

## 修改文件清单

### 后端
- `backend/src/main/java/com/studyroom/repository/ReservationRepository.java` - 新增查询方法
- `backend/src/main/java/com/studyroom/service/ReservationService.java` - 添加重复预约检查

### 前端
- `frontend/src/views/TimeSlotSelect.vue` - 显示已预约状态，禁用按钮

---

## 测试场景

### 场景 1：防止重复预约

1. 用户 A 登录系统
2. 预约"第一自习室"的"上午 8:00-12:00"
3. 预约成功，按钮变为"已预约"并禁用
4. 再次点击该时间段的预约按钮
5. 按钮已禁用，无法点击
6. 如果通过其他方式发起请求，后端会返回错误："您已经预约了该时间段，请勿重复预约"

### 场景 2：取消后可再次预约

1. 用户 A 预约了某个时间段
2. 进入"我的预约"取消该预约
3. 返回时间段列表
4. 按钮恢复为"立即预约"，可以再次预约
5. 预约成功

### 场景 3：不影响其他用户

1. 用户 A 预约了"上午 8:00-12:00"
2. 用户 B 登录系统
3. 用户 B 可以正常预约"上午 8:00-12:00"（因为是不同用户）
4. 预约成功

### 场景 4：不同时间段互不影响

1. 用户 A 预约了"上午 8:00-12:00"
2. 用户 A 可以继续预约"下午 14:00-18:00"（因为是不同时间段）
3. 预约成功

---

## 边界情况处理

### 1. 并发预约

**场景**：用户快速连续点击预约按钮

**处理**：
- 前端：预约请求发出后按钮立即禁用
- 后端：使用数据库事务和查询保证原子性
- 结果：只有第一次预约成功，后续请求被拦截

### 2. 网络延迟

**场景**：预约成功但网络延迟，用户再次点击

**处理**：
- 前端使用 async/await 等待请求完成
- 成功后立即更新状态和重新加载数据
- 按钮状态与服务器状态保持同步

### 3. 已取消的预约

**场景**：用户取消预约后想再次预约

**处理**：
- 取消时将状态设为 'CANCELLED'
- 查询时只统计 status='ACTIVE' 的预约
- 取消后可以再次预约同一时间段

---

## 数据一致性保证

### 数据库层面

```sql
-- 查询条件确保准确性
SELECT COUNT(*) 
FROM reservation 
WHERE user_id = ? 
  AND time_slot_id = ? 
  AND status = 'ACTIVE'
```

### 事务保证

```java
@Transactional
public Reservation createReservation(ReservationRequest request) {
    // 1. 检查重复
    // 2. 检查座位
    // 3. 创建预约
    // 4. 减少座位
    // 任何一步失败都会回滚
}
```

### 前后端同步

- 前端显示状态基于实时加载的预约记录
- 操作后立即重新加载最新数据
- 确保显示与数据库一致

---

## 用户体验改进

### 修复前
- 用户可以无限次预约同一时间段
- 按钮状态不反映实际情况
- 用户可能不知道自己已预约

### 修复后
- 已预约的时间段明确标识为"已预约"
- 按钮禁用，防止误操作
- 后端有兜底检查，确保数据正确
- 错误提示友好："您已经预约了该时间段，请勿重复预约"

---

## 部署和验证

### 重新部署

```bash
cd /Users/alex.ni/ai/studySpace

# 重新构建
docker compose build --no-cache

# 启动
docker compose up -d
```

### 验证步骤

1. **测试防止重复预约**
   - 登录系统
   - 预约一个时间段
   - 观察按钮变为"已预约"并禁用
   - 刷新页面，按钮仍显示"已预约"

2. **测试后端防护**
   - 打开浏览器开发者工具
   - 尝试手动发送重复预约请求
   - 应返回错误："您已经预约了该时间段，请勿重复预约"

3. **测试取消后再预约**
   - 取消一个已预约的时间段
   - 返回时间段列表
   - 按钮恢复为"立即预约"
   - 可以成功再次预约

4. **测试不同用户**
   - 用户 A 预约某时间段
   - 用户 B 登录
   - 用户 B 可以预约同一时间段

---

## 后续优化建议

### 1. 预约限制

可以考虑添加更多限制：
- 每个用户每天最多预约 N 个时间段
- 预约需要提前 X 小时
- 预约最多提前 Y 天

### 2. 预约历史

- 保留已取消的预约记录
- 显示预约历史和取消原因
- 统计用户的预约行为

### 3. 座位选择

- 允许用户选择具体座位号
- 显示座位地图
- 记住用户的座位偏好

---

**修复日期**：2026-01-22
**修复版本**：v1.1.1
**相关问题**：重复预约导致资源浪费
**修复状态**：✅ 已完成
