# 验证指南 - 三大改进

## 快速重新部署

```bash
cd /Users/alex.ni/ai/studySpace

# 停止旧容器
docker compose down

# 重新构建（确保使用最新代码）
docker compose build --no-cache

# 启动
docker compose up -d

# 查看日志
docker compose logs -f
```

等待约 1-2 分钟，直到所有服务启动完成。

---

## 验证 1：Toast 消息提示

### 测试步骤

1. 访问 http://localhost

2. **测试成功提示（绿色）**
   - 注册一个新账号
   - 应该在页面顶部看到绿色的"注册成功，请登录"提示
   - 3 秒后自动消失

3. **测试错误提示（红色）**
   - 使用错误密码登录
   - 应该看到红色的"用户名或密码错误"提示
   - 不应该出现浏览器原生 alert 弹窗

4. **测试警告提示（黄色）**
   - 未登录直接访问 http://localhost/study-rooms
   - 应该看到黄色的"请先登录"提示
   - 自动跳转到登录页

5. **测试操作成功提示**
   - 登录后预约一个座位
   - 看到绿色"预约成功"提示
   - 取消预约看到"取消成功"提示

### 预期结果

- ✅ 不再出现浏览器原生 alert 弹窗
- ✅ 所有提示都是页面顶部的 Toast 组件
- ✅ Toast 有颜色区分（绿/红/黄/蓝）
- ✅ Toast 有图标（✓ / ✕ / ⚠ / ℹ）
- ✅ Toast 3 秒后自动消失
- ✅ Toast 有淡入淡出动画

---

## 验证 2：座位数实时更新

### 测试步骤

1. **记录初始状态**
   - 登录系统
   - 进入"自习室列表"
   - 点击"第一自习室" -> "查看可预约时间"
   - 记录某个时间段的剩余座位数，例如"上午 8:00-12:00，剩余 50 个"

2. **测试预约减少座位**
   - 点击"立即预约"
   - 看到绿色"预约成功"提示
   - **关键：页面应该自动刷新，座位数立即变为 49**

3. **多次预约验证**
   - 用不同账号或时间段再预约几次
   - 每次预约后座位数都应减 1

4. **测试取消恢复座位**
   - 进入"我的预约"
   - 取消刚才的预约
   - 看到"取消成功"提示
   - 返回时间段列表，座位数应该恢复为 50

5. **测试座位满状态**
   - 找一个座位数少的时间段（如剩余 2 个）
   - 预约到剩余 0 个
   - "立即预约"按钮应该变灰，显示"已满"
   - 点击无效

### 预期结果

- ✅ 预约后座位数立即减少
- ✅ 取消预约后座位数立即恢复
- ✅ 座位数为 0 时按钮禁用
- ✅ 不会出现负数座位
- ✅ 不会出现超额预约

---

## 验证 3：友好的错误提示

### 测试步骤

1. **测试登录错误**
   - 输入正确用户名，错误密码
   - 应该看到："用户名或密码错误"
   - 不应该看到："400" 或 "Bad Request"

2. **测试网络错误**
   - 停止后端容器：`docker stop studyroom-backend`
   - 尝试登录
   - 应该看到："无法连接到服务器，请检查网络"
   - 重新启动：`docker start studyroom-backend`

3. **测试业务错误**
   - 注册一个已存在的用户名
   - 应该看到："用户名已存在"
   - 不应该看到错误码

4. **测试权限错误**
   - 用普通用户登录
   - 尝试访问 http://localhost/admin
   - 应该看到："需要管理员权限"
   - 自动跳转到自习室列表

5. **测试预约满错误**
   - 预约一个已满的时间段（如果有）
   - 应该看到："该时间段已满，无法预约"
   - 提示清晰明确

### 预期结果

- ✅ 所有错误提示都是中文
- ✅ 错误原因清晰明确
- ✅ 不出现 HTTP 状态码（400、500 等）
- ✅ 不出现英文错误信息
- ✅ 错误提示通过 Toast 显示，体验好

---

## 综合测试场景

### 场景 1：完整预约流程

1. 注册新用户 → 看到绿色成功提示
2. 登录 → 看到绿色"登录成功"
3. 查看自习室列表 → 点击进入
4. 选择时间段预约 → 看到"预约成功" + 座位数减少
5. 查看我的预约 → 看到刚才的预约记录
6. 取消预约 → 看到"取消成功" + 座位数恢复

### 场景 2：错误处理流程

1. 错误密码登录 → 红色错误提示
2. 重复注册相同用户名 → 红色"用户名已存在"
3. 预约已满时间段 → 红色"该时间段已满"
4. 普通用户访问管理后台 → 黄色"需要管理员权限"

### 场景 3：管理员操作流程

1. 用 admin/admin123 登录
2. 进入管理后台
3. 添加新自习室 → 绿色"添加成功"
4. 添加时间段 → 绿色"添加成功"
5. 删除自习室 → 先确认，再显示"删除成功"

---

## 验证清单

复制到本地，逐项验证：

```
[ ] Toast 提示替代了 alert
[ ] 成功操作显示绿色 Toast
[ ] 错误操作显示红色 Toast
[ ] 警告信息显示黄色 Toast
[ ] Toast 有平滑动画效果
[ ] Toast 3 秒后自动消失
[ ] 预约后座位数立即减少
[ ] 取消预约座位数恢复
[ ] 座位为 0 时按钮禁用
[ ] 错误提示是中文而非状态码
[ ] 网络错误提示友好
[ ] 权限错误提示明确
[ ] 所有错误都通过 Toast 显示
[ ] 不再出现浏览器原生 alert
[ ] 用户体验流畅顺畅
```

---

## 问题排查

### 如果 Toast 不显示

1. 检查浏览器控制台是否有错误
2. 确认 App.vue 中已添加 Toast 组件
3. 清除浏览器缓存，硬刷新（Ctrl+Shift+R）

### 如果座位数不更新

1. 检查后端日志：`docker logs studyroom-backend --tail 50`
2. 确认 ReservationService.java 已更新
3. 重新构建后端：`docker compose build --no-cache backend`

### 如果还是显示错误码

1. 检查 api/index.js 是否已更新拦截器
2. 清除浏览器缓存
3. 重新构建前端：`docker compose build --no-cache frontend`

---

## 验证完成

所有测试通过后，系统升级成功！

用户体验显著提升：
- 视觉更现代化
- 反馈更及时
- 提示更友好
- 操作更流畅

**完成时间**：_________
**验证人**：_________
**验证结果**：✅ 通过 / ❌ 失败
