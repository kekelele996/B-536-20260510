# 测试重复预约修复

## 快速测试步骤

### 1. 重新部署系统

```bash
cd /Users/alex.ni/ai/studySpace

# 重新构建
docker compose build --no-cache

# 启动
docker compose up -d

# 等待启动完成
sleep 60
```

---

## 测试场景

### 场景 1：基础防重复测试

**目标**：验证用户无法重复预约同一时间段

**步骤**：
1. 访问 http://localhost
2. 使用任意账号登录（或注册新账号）
3. 进入"自习室列表"
4. 点击任一自习室的"查看可预约时间"
5. 选择一个时间段，点击"立即预约"
6. 看到绿色"预约成功"提示
7. **关键检查**：该时间段的按钮应该变为"已预约"并变成红色
8. 按钮应该是禁用状态，无法再次点击

**预期结果**：
- ✅ 预约成功后按钮立即变为"已预约"
- ✅ 按钮颜色从绿色变为红色
- ✅ 按钮禁用，鼠标悬停时显示禁用样式
- ✅ 无法再次点击预约

---

### 场景 2：刷新页面验证

**目标**：验证刷新后状态仍然正确

**步骤**：
1. 完成场景 1 的预约
2. 刷新浏览器页面（F5 或 Cmd+R）
3. 重新进入该自习室的时间段列表

**预期结果**：
- ✅ 已预约的时间段仍显示"已预约"
- ✅ 按钮仍然是红色且禁用
- ✅ 状态持久化，不会因刷新而重置

---

### 场景 3：取消后再预约

**目标**：验证取消预约后可以再次预约

**步骤**：
1. 完成场景 1 的预约
2. 进入"我的预约"
3. 找到刚才的预约，点击"取消预约"
4. 确认取消
5. 看到绿色"取消成功"提示
6. 返回自习室时间段列表

**预期结果**：
- ✅ 取消成功后，该时间段的按钮恢复为"立即预约"
- ✅ 按钮颜色恢复为绿色
- ✅ 按钮可以再次点击
- ✅ 可以成功进行第二次预约

---

### 场景 4：后端防护测试（开发者测试）

**目标**：验证后端有防护机制

**步骤**：
1. 打开浏览器开发者工具（F12）
2. 切换到 Console 标签
3. 预约一个时间段
4. 在控制台执行以下代码尝试重复预约：

```javascript
// 获取当前用户 ID
const user = JSON.parse(localStorage.getItem('user'))

// 尝试重复预约（替换 timeSlotId 为实际的 ID）
fetch('/api/reservations', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'User-Id': user.id
  },
  body: JSON.stringify({
    userId: user.id,
    timeSlotId: 1  // 替换为实际已预约的时间段 ID
  })
})
.then(res => res.json())
.then(data => console.log(data))
```

**预期结果**：
- ✅ 返回错误响应
- ✅ code 为 400 或 500
- ✅ message 为 "您已经预约了该时间段，请勿重复预约"
- ✅ 控制台显示红色错误 Toast

---

### 场景 5：多用户测试

**目标**：验证不同用户可以预约同一时间段

**步骤**：
1. 用户 A 登录并预约"第一自习室"的"上午 8:00-12:00"
2. 用户 A 退出登录
3. 用户 B 登录（或注册新用户）
4. 用户 B 查看"第一自习室"的"上午 8:00-12:00"
5. 用户 B 点击"立即预约"

**预期结果**：
- ✅ 用户 B 可以成功预约（因为是不同用户）
- ✅ 预约成功后，座位数减少
- ✅ 用户 B 的按钮也变为"已预约"

---

### 场景 6：同一用户多个时间段

**目标**：验证用户可以预约不同时间段

**步骤**：
1. 登录系统
2. 预约"第一自习室"的"上午 8:00-12:00"
3. 继续预约"第一自习室"的"下午 14:00-18:00"
4. 再预约"第二自习室"的"上午 8:00-12:00"

**预期结果**：
- ✅ 所有预约都成功
- ✅ 每个预约都正常显示
- ✅ 只有已预约的时间段显示"已预约"
- ✅ 其他时间段仍可预约

---

## 验证清单

将以下内容复制到本地，逐项测试：

```
[ ] 预约成功后按钮变为"已预约"
[ ] 已预约按钮为红色且禁用
[ ] 刷新页面后状态保持
[ ] 无法重复预约同一时间段
[ ] 后端返回友好错误提示
[ ] 取消预约后可以再次预约
[ ] 不同用户可以预约同一时间段
[ ] 同一用户可以预约不同时间段
[ ] 控制台没有错误信息
[ ] Toast 提示正常显示
```

---

## 常见问题

### Q1: 按钮没有变为"已预约"？

**检查**：
- 刷新页面试试
- 查看浏览器控制台是否有错误
- 检查后端日志：`docker logs studyroom-backend --tail 50`

**解决**：
```bash
# 重新构建前端
docker compose build --no-cache frontend
docker compose restart frontend
```

### Q2: 仍然可以重复预约？

**检查**：
- 确认后端已更新：`docker logs studyroom-backend | grep "已预约"`
- 查看数据库预约记录

**解决**：
```bash
# 重新构建后端
docker compose build --no-cache backend
docker compose restart backend
```

### Q3: 取消预约后按钮没有恢复？

**检查**：
- 是否刷新了页面
- 预约是否真的取消成功

**解决**：
- 手动刷新页面（F5）
- 或重新进入时间段列表页面

---

## 测试成功标志

当所有场景都通过时，你应该看到：

1. ✅ 每个用户每个时间段只能预约一次
2. ✅ 已预约的时间段有明确标识
3. ✅ 系统防止了重复预约
4. ✅ 错误提示友好明确
5. ✅ 取消后可以再次预约
6. ✅ 不影响正常预约流程

---

**测试完成后**，你可以放心地将系统交付使用了！

**测试日期**：_________
**测试人**：_________
**测试结果**：✅ 通过 / ❌ 失败（问题描述：_________）
